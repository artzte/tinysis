<%
  m = contract_item_month
  if @this_month < m or c.statusable_months.include?(m) == false
    val = image_tag('dash.gif')
  elsif @status_missing[c].include?(m)
    val = image_tag('question.gif')
  else
    val = image_tag('check.gif')
  end 
-%>
<%= content_tag 'td', val %>